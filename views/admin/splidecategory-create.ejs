<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/adminpanel-head') %>
  </head>
  <body>
    <%- include('../partials/adminpanel-bar') %>

    <main id="main" class="main">
      <div class="container mt-3">
        <div class="row justify-content-center">
          <div class="col-lg-6 mt-5">
            <form
              method="post"
              enctype="multipart/form-data"
              action="/admin/splidecategory/create"
            >
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <div class="mb-3 row">
                <label for="name" class="col-sm-4 col-form-label"
                  >Kategori İsmi</label
                >
                <div class="col-sm-8">
                  <input
                    type="text"
                    class="form-control"
                    id="name"
                    name="name"
                  />
                </div>
              </div>

              <div class="mb-3 row">
                <label for="resim" class="col-sm-4 col-form-label">Resim</label>
                <div class="col-sm-8">
                  <input
                    type="file"
                    class="form-control"
                    id="resim"
                    name="resim"
                  />
                </div>
              </div>

              <div class="mb-3 row">
                <label for="kategori" class="col-sm-4 col-form-label"
                  >Kategori</label
                >
                <div class="col-sm-8">
                  <select name="kategori" id="kategori" class="form-select">
                    <option value="-1" selected>Seçiniz</option>
                    <% categories.forEach(category => { %>
                    <option value="<%= category.headcategoryid %>">
                      <%= category.name %>
                    </option>
                    <% }) %>
                  </select>
                </div>
              </div>

              <div class="mb-3 d-grid gap-2 d-md-flex justify-content-md-end">
                <button type="submit" class="btn btn-primary">Kaydet</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </main>

    <script src="/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
  </body>
</html>
