<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head') %>
  </head>
  <body>
    <%- include('../partials/nav') %>

    <div
      class="container mt-5 flex flex-col space-y-6 md:flex-row md:justify-center"
    >
      <div class="max-w-md flex-1 lg:mr-6 mx-10 lg:mx-0">
        <h2 class="font-poppins font-semibold text-sm mb-5">OTURUM AÇ</h2>
        <p class="font-poppins font-light text-xs mb-5">
          Giriş yapmak için e-posta adresini ve parolanı gir.
        </p>
        <% if (typeof message != "undefined") { %>
        <div class="<%= message.class %>"><%= message.text %></div>
        <% } %>

        <form method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="mb-3 flex flex-col">
            <label for="email" class="text-sm font-medium text-gray-700 mb-1"
              >E-posta</label
            >
            <input
              type="email"
              class="form-input rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
              id="email"
              name="email"
            />
          </div>

          <div class="mb-3 flex flex-col">
            <label for="password" class="text-sm font-medium text-gray-700 mb-1"
              >Parola</label
            >
            <input
              type="password"
              class="form-input rounded-md border-gray-300 shadow-sm focus:border-indigo-300 focus:ring focus:ring-indigo-200 focus:ring-opacity-50"
              id="password"
              name="password"
            />
          </div>

          <div class="mb-3">
            <a
              href="reset-password"
              class="text-black text-xs font-medium inline-flex justify-center py-1 font-sans"
              >Parolanı mı unuttun?</a
            >
            <button
              type="submit"
              class="bg-black text-xs text-white font-medium py-1 font-poppins custom-text-shadow rounded-full cursor-pointer h-9 w-full hover:bg-white hover:text-black hover:border-black border border-transparent transition duration-300 ease-in-out"
            >
              Oturum Aç
            </button>
          </div>
        </form>
      </div>

      <div class="max-w-md flex-1 lg:max-w-sm mx-10 lg:mx-0">
        <h2 class="font-poppins font-semibold text-xs mb-5">Hesabın yok mu?</h2>
        <p class="font-poppins font-light text-xs mb-5">
          Henüz Raviquer'de kullanıcı hesabın yoksa kayıt formuna ulaşmak için
          bu seçeneği kullanabilirsin.
        </p>
        <p class="font-poppins font-light text-xs mb-5">
          Kayıt sürecini hızlandırmak için senden gerekli bilgileri vermeni
          isteyeceğiz.
        </p>
        <a href="/account/register">
          <button
            class="bg-white text-xs text-black font-medium py-1 font-poppins custom-text-shadow rounded-full cursor-pointer h-9 w-full hover:bg-black hover:text-white hover:border-transparent border border-black transition duration-300 ease-in-out"
          >
            Hesap Oluştur
          </button>
        </a>
      </div>
    </div>

    <%- include('../partials/footer') %>
    <script src="/static/js/navanimation.js"></script>
  </body>
</html>
